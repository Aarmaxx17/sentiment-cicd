apiVersion: v1
kind: Service

metadata:
  name: sentiment-service

spec:
  selector:
    app: sentiment-app

  type: NodePort

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30008
